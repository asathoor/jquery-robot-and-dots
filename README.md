context.fillStyle = 'green';
      context.fill();
      context.lineWidth
      
       context.fillStyle = 'green';
      context.fill();
      context.lineWidth 
      
      context.fillStyle = 'green';
      context.fill();
      context.lineWidth